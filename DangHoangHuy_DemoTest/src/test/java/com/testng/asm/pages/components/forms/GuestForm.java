package com.testng.asm.pages.components.forms;

import org.openqa.selenium.WebDriver;

public class GuestForm extends BaseForm{
    public GuestForm(WebDriver driver) {
        super(driver);
    }
}
